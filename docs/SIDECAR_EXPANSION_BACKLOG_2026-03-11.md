# SIDECAR 확장 백로그 / 반영 검토 리스트 (2026-03-11)

Doc-Tier: P3 (Archive/Reference)


## 결정 요약
- P2(주문 라이프사이클 상태기계) 기본 구현은 완료.
- 아래 2개 항목은 **스킵이 아니라 P3 필수 항목으로 이관**:
  1) 데이터 품질 가드
  2) 히스테리시스 + 최소 유지시간

즉, 지금은 P2 범위를 안정적으로 닫고, P3에서 리스크 가드 품질을 올리는 순서로 진행한다.

---

## 반영 검토 표

| ID | 항목 | 우선순위 | 반영 단계 | 상태 | 지금 안 넣는 이유 | 반영 완료 기준 |
|---|---|---|---|---|---|---|
| X1 | 데이터 품질 가드 (VIX/지수 소스 불일치, 지연, 누락 점수화) | Must | P3-1 | 반영(1차) | P2 범위(상태기계)와 책임 분리 필요 | 품질 점수 낮을 때 자동 보수모드 전환 + 로그/텔레그램 코드화 |
| X2 | 히스테리시스 + 최소 유지시간 | Must | P3-1 | 반영(1차) | 임계치 흔들림 방지 로직은 감시잡(P3)에 포함하는 것이 자연스러움 | 레벨 진입/해제 임계 분리 + 최소 유지시간 + 쿨다운 동작 검증 |
| X3 | 상관 급등(클러스터 리스크) | Should | P3-2 | 보류 | 신호 품질/데이터 의존성 증가 | 포지션 집단 동조 하락 시 레벨 가중치 반영 |
| X4 | 포지션 Heat(총 오픈리스크/자본) | Should | P3-2 | 보류 | 계좌/포지션 데이터 결합 및 룰 검증 필요 | Heat 초과 시 신규 진입 제한/축소 자동화 |
| X5 | 이벤트 캘린더(CPI/FOMC/대형실적) | Could | P3-3 | 보류 | 외부 캘린더 소스 정합성/운영 복잡도 증가 | 이벤트 윈도우에서 가드 강화 규칙 적용 |

---

## P3 시작 전 검토 포인트
1. X1/X2 1차 반영본 기준으로 `observe` 모드에서 최소 3~5거래일 로그 수집
2. 품질 점수 하락 시 강제 보수모드/신규진입 차단이 의도대로 동작하는지 확인
3. 히스테리시스 오탐 방지 지표(전환 횟수, 유지시간 위반, 불필요 차단률) 확인 후 `active` 전환

---

## P3-2 진행 메모 (장중 감시잡/L1~L3)
- 상태: **진행중(1차 구현)**
- 반영 범위:
  - `src/market-guard.ts` 신규 추가
  - `sidecar-market-guard` 워크플로우(평일 5분)
  - `state/market-guard-state.json`, `state/guard-action-ledger.json`, `state/last-market-guard.json`
- 자동화 원칙:
  - 임계치는 env를 `base guardrail`로 사용
  - 런타임에서 profile/quality에 따라 effective threshold 자동 보정
  - de-escalate hold + action cooldown으로 플래핑 억제

---

## 운영 원칙
- 분석 엔진(`US_Alpha_Seeker`)은 변경하지 않고 sidecar에서만 확장.
- 새 신호는 반드시 `observe -> active` 2단계 롤아웃.
- 신규 항목은 모두 환경변수/Step Summary/Artifact로 추적 가능해야 함.
